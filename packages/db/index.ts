/**
 * Shared Convex DB package.
 * - client: createConvexClient(url) for ConvexProvider
 * - API: import from your app's convex folder (apps/web/convex/api or root convex/_generated after codegen)
 */
export { createConvexClient } from "./client";
