# packages/app

Screens, auth, and db client. See [root AGENTS.md](../../AGENTS.md).

- Compose `@project-template/ui` for all UI
- Use Solito for navigation in screens
- Export new screens from `index.ts`
